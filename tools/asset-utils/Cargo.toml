[package]
name = "asset-utils"
version = "0.1.0"
edition = "2021"

[lib]
name = "asset_utils"
path = "src/lib.rs"

[dependencies]
symphonia = { version = "0.5.4", features = ["all"] }
rubato = "0.12"
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
