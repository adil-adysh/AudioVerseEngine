{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug (LLDB) - current crate",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/target/debug/${workspaceFolderBasename}",
      "args": [],
      "cwd": "${workspaceFolder}"
    }
  ]
}
